<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Job Guarantee Platform</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
  <style>
    body {
      background: linear-gradient(135deg, #e0e7ff, #f3f4f6);
    }

    /* Glass card */
    .glass-card {
      background: rgba(255, 255, 255, 0.15);
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      border-radius: 1.5rem;
      border: 1px solid rgba(255, 255, 255, 0.2);
      box-shadow: 0 8px 25px rgba(0,0,0,0.12);
      transition: transform 0.4s ease, box-shadow 0.4s ease;
    }
    .glass-card:hover {
      transform: scale(1.03);
      box-shadow: 0 18px 40px rgba(0,0,0,0.2);
    }

    .btn-gradient {
      background: linear-gradient(135deg, #4f46e5, #6366f1);
    }
    .btn-gradient:hover {
      background: linear-gradient(135deg, #6366f1, #4f46e5);
    }

    h1 span {
      display: inline-block;
      animation: bounceY 2s infinite;
    }
    @keyframes bounceY {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-5px); }
    }
  </style>
</head>
<body class="text-gray-800 font-sans">

  <!-- Header -->
  <header class="text-center py-10 px-5 md:py-16">
    <h1 class="text-4xl md:text-5xl font-extrabold mb-3">
      <span>Job Guarantee</span> Platform
    </h1>
    <p class="text-base md:text-lg max-w-2xl mx-auto text-gray-700">
      Choose a plan, post free requests, or hire talent easily!
    </p>
  </header>

  <main class="container mx-auto px-5 space-y-16 md:space-y-20">

    <!-- Section 1: Paid Plan -->
    <section class="text-center">
      <h2 class="text-2xl md:text-3xl font-bold mb-6 text-gray-800">30 Days Paid Plan</h2>
      <div class="flex flex-col md:flex-row justify-center gap-6">
        <div class="glass-card p-6 w-full max-w-md mx-auto">
          <h3 class="text-xl md:text-2xl font-bold mb-1 text-gray-800">30 Days Plan</h3>
          <p class="text-gray-600 mb-3">Get a job in 30 days</p>
          <h4 class="text-2xl md:text-3xl font-extrabold text-green-600 mb-4">₹1</h4>
          <form action="/login/gurrenty/plan30" method="GET">
            <input type="hidden" name="plan" value="30 Days">
            <input type="hidden" name="amount" value="9">
            <button class="w-full py-2.5 rounded-xl font-semibold text-white btn-gradient shadow-lg hover:shadow-2xl transition duration-300 text-base md:text-lg">
              Buy Now <i class="fa-solid fa-arrow-right ml-2"></i>
            </button>
          </form>
        </div>
      </div>
    </section>

    <!-- Section 2: Free Job Requests -->
    <section class="text-center">
      <h2 class="text-2xl md:text-3xl font-bold mb-2 text-gray-800">Free Job Requests</h2>
      <p class="text-gray-600 mb-4 max-w-xl mx-auto">
        Post your job requirement for free. All employers can see and contact you!
      </p>
      <div class="flex justify-center">
        <a href="http://localhost:3000/login/gurrenty/freepost" 
           class="py-2.5 md:py-3 px-6 md:px-10 rounded-2xl font-bold text-base md:text-lg shadow-lg hover:shadow-2xl btn-gradient transform hover:scale-105 transition duration-300">
          Post a Free Job Request <i class="fa-solid fa-paper-plane ml-2"></i>
        </a>
      </div>
    </section>

    <!-- Section 3: Job Providers -->
    <section class="text-center">
      <h2 class="text-2xl md:text-3xl font-bold mb-2 text-gray-800"> Post a Job / Hire Talent</h2>
      <p class="text-gray-600 mb-4 max-w-xl mx-auto">
        If you are a company or recruiter, post your job offers here to reach potential candidates.
      </p>
      <div class="flex justify-center">
        <a href="http://localhost:3000/login/gurrenty/jobpost" 
           class="py-2.5 md:py-3 px-6 md:px-10 rounded-2xl font-bold text-base md:text-lg shadow-lg hover:shadow-2xl btn-gradient transform hover:scale-105 transition duration-300">
          Post a Job Offer <i class="fa-solid fa-briefcase ml-2"></i>
        </a>
      </div>
    </section>

  </main>

  <!-- Footer -->
  <footer class="text-center py-6 md:py-10 text-gray-500">
    &copy; 2025 Job Guarantee Platform. All rights reserved.
  </footer>

</body>
</html>
